# 📊 Visual Architecture & Flow Diagrams

## System Architecture Diagram

```
┌──────────────────────────────────────────────────────────────────┐
│                        CLIENT BROWSER                            │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │           React SPA (Frontend)                             │  │
│  │  ┌──────────────────────────────────────────────────────┐  │  │
│  │  │ ProjectDashboard                                     │  │  │
│  │  │  ├─ SprintBurndownChart                             │  │  │
│  │  │  ├─ StatusOverview                                  │  │  │
│  │  │  └─ ForYouSection, UpcomingWork                     │  │  │
│  │  └──────────────────────────────────────────────────────┘  │  │
│  │  ┌──────────────────────────────────────────────────────┐  │  │
│  │  │ WorkItemDetailPage                                   │  │  │
│  │  │  └─ SubTaskPanel                                    │  │  │
│  │  │      ├─ List & Manage Children                      │  │  │
│  │  │      └─ SubTaskCreationModal                        │  │  │
│  │  └──────────────────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────┬───────────────────────────────────────────────┘
                   │ HTTP REST API (JSON)
┌──────────────────▼───────────────────────────────────────────────┐
│                      EXPRESS.JS SERVER                           │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │ Route Layer (/api/work-items/*, /api/sprints/*)          │  │
│  │  GET/POST/PATCH/DELETE routes with auth middleware       │  │
│  └──────────────────┬───────────────────────────────────────┘  │
│                     │                                            │
│  ┌──────────────────▼───────────────────────────────────────┐  │
│  │ Service Layer (Business Logic)                           │  │
│  │  ┌─ WorkItemService                                     │  │
│  │  │  ├─ createSubtask()                                  │  │
│  │  │  ├─ updateChildStatus() + auto-update parent         │  │
│  │  │  ├─ deleteWorkItem()                                 │  │
│  │  │  └─ ...10 more methods                               │  │
│  │  └─ BurndownService                                     │  │
│  │     ├─ calculateBurndown()                              │  │
│  │     ├─ getTeamVelocity()                                │  │
│  │     └─ getVelocityTrend()                               │  │
│  └──────────────────┬───────────────────────────────────────┘  │
│                     │                                            │
│  ┌──────────────────▼───────────────────────────────────────┐  │
│  │ Middleware Layer                                         │  │
│  │  ├─ authMiddleware (verify token)                        │  │
│  │  ├─ errorMiddleware (error handling)                     │  │
│  │  └─ validation (request validation)                      │  │
│  └──────────────────┬───────────────────────────────────────┘  │
│                     │                                            │
│  ┌──────────────────▼───────────────────────────────────────┐  │
│  │ Model Layer (Mongoose ODM)                              │  │
│  │  ├─ Task Model                                          │  │
│  │  │  ├─ Schema with parentTask field                     │  │
│  │  │  ├─ Async validators                                 │  │
│  │  │  ├─ Pre-save middleware                              │  │
│  │  │  ├─ Helper methods (getChildren, etc)                │  │
│  │  │  └─ 6 compound indexes                               │  │
│  │  ├─ ActivityLog Model (audit trail)                     │  │
│  │  └─ Sprint Model (for burndown)                         │  │
│  └──────────────────┬───────────────────────────────────────┘  │
└──────────────────┬─┴───────────────────────────────────────────┘
                   │ MongoDB Driver (Query/Command Protocol)
┌──────────────────▼───────────────────────────────────────────────┐
│                    MONGODB DATABASE                              │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │ Collections:                                               │  │
│  │  - tasks (with parent-child relationships)                │  │
│  │  - activitylogs (audit trail)                             │  │
│  │  - sprints (for burndown data)                            │  │
│  │                                                             │  │
│  │ Indexes Created:                                           │  │
│  │  - { parentTask: 1, project: 1 }                          │  │
│  │  - { parentTask: 1, status: 1 }                           │  │
│  │  - { project: 1, status: 1 }                              │  │
│  │  - { sprint: 1, status: 1 }                               │  │
│  │  - { assignedTo: 1, project: 1 }                          │  │
│  │  - And others for performance                             │  │
│  └────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Component Hierarchy

```
ProjectDashboard (Root)
├── AIInsightBanner
├── ForYouSection
├── SprintBurndownChart
│   ├── Line Chart (Chart.js)
│   ├── Stats Cards
│   └── Health Indicators
├── StatusOverview
│   ├── Doughnut Chart (Chart.js)
│   ├── Status Breakdown Grid
│   └── Completion Progress
├── StatusChart (Issue Status)
├── TeamWorkload
└── UpcomingWorkCard

WorkItemDetailPage (or any detail view)
├── TaskHeader
├── TaskDescription
├── SubTaskPanel
│   ├── ExpandToggle
│   ├── StoryPointsBadge
│   ├── ChildrenStats
│   ├── AddSubTaskButton
│   └── ChildrenList
│       └── ChildItem (repeating)
│           ├── ChildTitle & Key
│           ├── StatusDropdown
│           ├── DeleteButton
│           └── Description
│
└── SubTaskCreationModal
    ├── ModalHeader
    ├── Form
    │   ├── TitleInput
    │   ├── DescriptionTextarea
    │   ├── StoryPointsInput
    │   ├── PrioritySelect
    │   ├── AssignToInput
    │   └── DueDateInput
    ├── ValidationErrors
    └── FormActions (Cancel/Create)
```

---

## Data Flow: Create Sub-Task

```
┌─────────────────────────────────────────────────────────────┐
│ 1. User Interaction                                         │
│ User clicks "Add Sub-Task" → SubTaskPanel shows modal       │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│ 2. Form Input                                               │
│ User fills: title, story points, priority, etc.             │
│ Modal validates: title required, points 0-100, etc.         │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│ 3. API Request                                              │
│ POST /api/work-items/:parentId/subtasks                     │
│ {                                                            │
│   "title": "Fix login",                                     │
│   "storyPoints": 5,                                         │
│   "priority": "high"                                        │
│ }                                                            │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│ 4. Backend Processing                                       │
│ ├─ AuthMiddleware: Verify token ✓                           │
│ ├─ Route Handler: Parse request                             │
│ ├─ Service Layer:                                           │
│ │  ├─ Validate parent exists                                │
│ │  ├─ Prevent epic parent                                   │
│ │  ├─ Check nesting depth                                   │
│ │  └─ Create document                                       │
│ └─ Database Write: Save to tasks collection                 │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│ 5. Parent Auto-Update                                       │
│ ├─ Evaluate parent status:                                  │
│ │  ├─ Get all children                                      │
│ │  └─ Determine new parent status                           │
│ ├─ Update parent in database                                │
│ └─ Log activity                                             │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│ 6. API Response                                             │
│ {                                                            │
│   "success": true,                                          │
│   "data": {                                                  │
│     "_id": "...",                                           │
│     "title": "Fix login",                                   │
│     "parentTask": "...",                                    │
│     "status": "todo"                                        │
│   }                                                          │
│ }                                                            │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│ 7. Frontend Update                                          │
│ ├─ Close modal                                              │
│ ├─ Refresh children list                                    │
│ ├─ Update story points display                              │
│ └─ Show success message                                     │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│ 8. User Sees Result                                         │
│ New subtask appears in SubTaskPanel list ✅                  │
└─────────────────────────────────────────────────────────────┘
```

---

## Data Flow: Update Child Status

```
┌──────────────────────────────────────────────────────────────┐
│ 1. User Changes Status                                       │
│ User selects "In Progress" from status dropdown              │
└──────────────────┬───────────────────────────────────────────┘
                   │
┌──────────────────▼───────────────────────────────────────────┐
│ 2. API Request                                               │
│ PATCH /api/work-items/:childId/status                        │
│ { "status": "in_progress" }                                  │
└──────────────────┬───────────────────────────────────────────┘
                   │
┌──────────────────▼───────────────────────────────────────────┐
│ 3. Service: Update Child                                     │
│ ├─ Update child.status = "in_progress"                       │
│ ├─ Save to database                                          │
│ └─ Get parent ID                                             │
└──────────────────┬───────────────────────────────────────────┘
                   │
┌──────────────────▼───────────────────────────────────────────┐
│ 4. Service: Evaluate Parent Status                           │
│                                                               │
│ Query: Get all children of parent                            │
│ ├─ Child 1: done     ✓                                       │
│ ├─ Child 2: in_progress  ← JUST UPDATED                      │
│ └─ Child 3: todo                                             │
│                                                               │
│ Apply Logic:                                                 │
│ if (anyChildInProgress)  →  Parent = "in_progress"           │
│                                                               │
│ Result: Parent status becomes "in_progress"                  │
└──────────────────┬───────────────────────────────────────────┘
                   │
┌──────────────────▼───────────────────────────────────────────┐
│ 5. Update Parent in DB                                       │
│ parent.status = "in_progress"                                │
│ parent.updatedAt = now()                                     │
│ Save to database                                             │
└──────────────────┬───────────────────────────────────────────┘
                   │
┌──────────────────▼───────────────────────────────────────────┐
│ 6. Log Activity                                              │
│ ActivityLog.create({                                         │
│   actionType: "subtask_status_changed",                      │
│   actor: userId,                                             │
│   entityId: childId,                                         │
│   message: "Status changed to in_progress"                   │
│ })                                                            │
└──────────────────┬───────────────────────────────────────────┘
                   │
┌──────────────────▼───────────────────────────────────────────┐
│ 7. API Response                                              │
│ {                                                             │
│   "success": true,                                           │
│   "child": { ... updated child ... },                        │
│   "parent": { ... updated parent ... }                       │
│ }                                                             │
└──────────────────┬───────────────────────────────────────────┘
                   │
┌──────────────────▼───────────────────────────────────────────┐
│ 8. Frontend Updates                                          │
│ ├─ Update child status in list                               │
│ ├─ Update parent status display                              │
│ ├─ Refresh story points                                      │
│ ├─ Update status counters                                    │
│ └─ Update burndown chart                                     │
└──────────────────┬───────────────────────────────────────────┘
                   │
┌──────────────────▼───────────────────────────────────────────┐
│ 9. User Sees Result                                          │
│ ✓ Child status updated to "In Progress"                      │
│ ✓ Parent status automatically updated to "In Progress"       │
│ ✓ Status Overview counters updated                           │
│ ✓ Burndown chart recalculates                                │
└──────────────────────────────────────────────────────────────┘
```

---

## Parent Status Auto-Update Logic

```
                    Parent Status Rules
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   Check 1:            Check 2:           Check 3:
   All Done?          In Progress?        In Review?
        │                  │                  │
       YES                YES                YES
        │                  │                  │
        ↓                  ↓                  ↓
   Parent = DONE     Parent = IN_PROGRESS  Parent = IN_REVIEW
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
                    No More Checks
                           │
                    Default: Parent = TODO
```

**Applied in this order (first match wins):**

```javascript
if (allChildrenDone())          → Parent = "done"      ✅
else if (anyChildInProgress())  → Parent = "in_progress" 🔄
else if (anyChildInReview())    → Parent = "in_review"   👀
else                            → Parent = "todo"        📋
```

---

## Story Points Aggregation

```
Parent Task (5 points)
│
├─ Child 1: 3 points
├─ Child 2: 2 points
└─ Child 3: 5 points
│
Total: 5 (own) + 10 (children) = 15 points

Display:
┌─────────────────────────┐
│ Points: 5 + 10 = 15     │
│ Own: 5    Children: 10  │
└─────────────────────────┘

Calculation:
Total = Parent.storyPoints + SUM(Child.storyPoints for all children)
```

---

## Burndown Chart Data Generation

```
Sprint Timeline: 10 days

Day 1  Day 2  Day 3  Day 4  Day 5  Day 6  Day 7  Day 8  Day 9  Day 10
 │      │      │      │      │      │      │      │      │      │
 20                                                               0
  │\                                                             /
  │ \                                                           / (Ideal)
  │  \                                                         /
  │   \────────────────────────────────────────────────────/
  │      20, 19, 17, 16, 14, 13, 11, 10, 8, 7 (Actual)
  │
  Health Status:
  - If Actual ≤ Ideal × 1.1  → "healthy" ✅
  - If Actual > Ideal × 1.1   → "at-risk" ⚠️

Trend Analysis (last 5 days):
  Recent burn rate: Points remaining per day
  - If improving: Velocity increasing (good!)
  - If worsening: Velocity decreasing (watch!)
  - If stable: Steady pace

Forecast:
  Days needed = Remaining Points / Avg Burn Rate
  Will complete on time? Yes/No
```

---

## Validation Layers

```
┌─────────────────────────────────────────────────────┐
│ Layer 1: Frontend Form Validation                   │
│ ├─ Title required & < 255 chars                     │
│ ├─ Story points 0-100                               │
│ ├─ Due date not in past                             │
│ └─ Show error messages                              │
└──────────────┬──────────────────────────────────────┘
               │ HTTP Request
┌──────────────▼──────────────────────────────────────┐
│ Layer 2: Route Handler Validation                   │
│ ├─ Required fields present                          │
│ ├─ Data types correct                               │
│ ├─ Length constraints met                           │
│ └─ Return 400 if invalid                            │
└──────────────┬──────────────────────────────────────┘
               │
┌──────────────▼──────────────────────────────────────┐
│ Layer 3: Service Validation                         │
│ ├─ Parent exists                                    │
│ ├─ Parent not epic                                  │
│ ├─ No deep nesting                                  │
│ └─ No orphaning                                     │
└──────────────┬──────────────────────────────────────┘
               │
┌──────────────▼──────────────────────────────────────┐
│ Layer 4: Model Schema Validation                    │
│ ├─ Async parent validator                           │
│ ├─ Enum validation for status                       │
│ ├─ Type validation for fields                       │
│ └─ Pre-save middleware checks                       │
└──────────────┬──────────────────────────────────────┘
               │
           ✅ Database Write
```

---

## Entity Relationship Diagram

```
┌─────────────────┐
│      TASK       │
├─────────────────┤
│ _id             │
│ title           │
│ description     │
│ parentTask ────┐│  (Optional Foreign Key)
│ issueType      ││  (Enum: task, subtask, bug, epic, story)
│ status         ││  (Enum: todo, in_progress, in_review, done)
│ storyPoints    ││
│ project        │├─→ PROJECT
│ sprint         ││
│ assignedTo     ││
│ createdAt      ││
│ updatedAt      ││
└─────────────────┘│
                   │
        ┌──────────┘
        │
        │ (Self-Reference)
        │ (One Parent → Many Children)
        │ (Max 1 level nesting)
        │
        ↓
┌─────────────────────────────────────────┐
│  Task (as Parent)                       │
│  ├─ Child Task 1 (parentTask = parent)  │
│  ├─ Child Task 2 (parentTask = parent)  │
│  └─ Child Task 3 (parentTask = parent)  │
│                                         │
│  Constraints:                           │
│  ├─ Parent cannot be epic               │
│  ├─ Parent cannot be another subtask    │
│  └─ Max depth = 1                       │
└─────────────────────────────────────────┘

┌──────────────────────┐
│   ACTIVITY LOG       │
├──────────────────────┤
│ _id                  │
│ actionType  ────────→ Subtask operations
│ actor               │  (subtask_created, moved, etc.)
│ entityId    ────────→ Task ID
│ entityType          │
│ message             │
│ details             │
│ createdAt           │
└──────────────────────┘
```

---

## Request/Response Examples

### Create Sub-Task

**Request:**
```
POST /api/work-items/parent123/subtasks
Content-Type: application/json
Authorization: Bearer token

{
  "title": "Implement login form",
  "description": "Create user login form component",
  "storyPoints": 5,
  "priority": "high",
  "assignedTo": "user456",
  "dueDate": "2026-01-20"
}
```

**Response (201 Created):**
```json
{
  "success": true,
  "data": {
    "_id": "subtask789",
    "title": "Implement login form",
    "parentTask": "parent123",
    "issueType": "subtask",
    "status": "todo",
    "storyPoints": 5,
    "priority": "high",
    "assignedTo": "user456",
    "project": "proj123",
    "issueKey": "PROJ-102",
    "createdAt": "2026-01-10T12:00:00Z",
    "updatedAt": "2026-01-10T12:00:00Z"
  }
}
```

### Update Status with Parent Cascade

**Request:**
```
PATCH /api/work-items/subtask789/status
Content-Type: application/json
Authorization: Bearer token

{ "status": "in_progress" }
```

**Response:**
```json
{
  "success": true,
  "child": {
    "_id": "subtask789",
    "status": "in_progress",
    "updatedAt": "2026-01-10T12:05:00Z"
  },
  "parent": {
    "_id": "parent123",
    "status": "in_progress",  ← AUTO-UPDATED
    "updatedAt": "2026-01-10T12:05:00Z"
  }
}
```

---

**Diagrams Created:** 8  
**Visual Elements:** Complete system architecture  
**Data Flows:** 2 detailed examples  
**Validation Flow:** Multi-layer visualization  
**Entity Relationships:** Clear ERD  

All visual documentation ready! 📊✅
